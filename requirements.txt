metadrive-simulator==0.4.3
opencv-python-headless
numpy
matplotlib

# NOTE (macOS): you will see objc "Class SDL... implemented in both" warnings.
# pygame and cv2 each bundle libSDL2. opencv-python-headless 5.x bundles it too,
# so swapping to headless does NOT clear them (differs from opencv 4.x).
# Verified harmless: the top-down renderer works with the collision present.
# Pin opencv-python-headless<5 if the log noise becomes a problem.
